jenkins:
  nodes:
    - mode: "EXCLUSIVE"
      name: "nebula-jenkins-agent1.local.mainnet.community"
      remoteFS: "/jenkins"
      retentionStrategy: "always"
      permanent:
        labelString: "internal snapshot-testing medium 4vcpu-8gb"
        launcher:
          inbound:
            webSocket: true
            workDirSettings:
              disabled: false
              failIfWorkDirIsMissing: false
              internalDir: "remoting"
    - mode: "EXCLUSIVE"
      name: "nebula-jenkins-agent2.local.mainnet.community"
      remoteFS: "/jenkins"
      retentionStrategy: "always"
      permanent:
        labelString: "internal snapshot-testing medium 4vcpu-8gb"
        launcher:
          inbound:
            webSocket: true
            workDirSettings:
              disabled: false
              failIfWorkDirIsMissing: false
              internalDir: "remoting"
    - mode: "EXCLUSIVE"
      name: "nebula-jenkins-agent3.local.mainnet.community"
      remoteFS: "/jenkins"
      retentionStrategy: "always"
      permanent:
        labelString: "internal snapshot-testing medium 4vcpu-8gb"
        launcher:
          inbound:
            webSocket: true
            workDirSettings:
              disabled: false
              failIfWorkDirIsMissing: false
              internalDir: "remoting"
    - mode: "EXCLUSIVE"
      name: "nebula-jenkins-agent4.local.mainnet.community"
      remoteFS: "/jenkins"
      retentionStrategy: "always"
      permanent:
        labelString: "internal snapshot-testing medium 4vcpu-8gb"
        launcher:
          inbound:
            webSocket: true
            workDirSettings:
              disabled: false
              failIfWorkDirIsMissing: false
              internalDir: "remoting"